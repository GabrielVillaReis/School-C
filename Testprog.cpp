#include <stdio.h>
int main(){
	float x,a;
	printf("Digite o preço de um produto:");
	scanf("%f",&x);
	a = x;
	switch(x){
		case (a>100) = x+(x/5);break;
		case (a<100): x = x+(x/10);
	}
	return 0;
}
