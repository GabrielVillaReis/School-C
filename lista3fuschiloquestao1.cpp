#include <stdio.h>
int main (){
	char str[100];
	int i;
	int num = 0;
	printf("Digite uma string de ate 100 caracteres: \n");
	gets (str);	
	for(i == 0; str[i] != '\0'; i++){
	}
	int tamanho = i;
	if (str[0] == 65 || str[0] == 97){
		printf("As 4 primeiras letras são:%c %c %c %c\n",str[0],str[1],str[2],str[3]);
	}
	else{
}
	printf("tamanho = %d", tamanho);
	return 0;
}
