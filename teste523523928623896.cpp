#include <stdio.h>
#include <string.h>
int main (){
	char str[100];
	char strinv[100];
	int i, tamanho;
	int count = 1;
	printf("Digite uma string de ate 100 caracteres: ");
	gets(str);
	for (i == 0; str[i] != '\0'; i++){
	}
	strcpy (strinv, str);
	strrev (strinv);
	printf("%s",strinv);
return 0;
}
